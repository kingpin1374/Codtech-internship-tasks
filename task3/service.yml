apiVersion: v1
kind: Service
metadata:
  name: uniform-wala-service
spec:
  type: LoadBalancer # Or NodePort if testing locally
  selector:
    app: uniform-wala
  ports:
    - protocol: TCP
      port: 80         # Port accessible by users
      targetPort: 9000 # Port the Flask app is listening ony
      
